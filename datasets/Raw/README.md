# Raw Data

Unmodified sensor recordings exactly as captured from the ESP32 (see `tinyml/Data_Collection/log_serial_to_csv.py`), before any filtering or normalization.

**Status:** Empty — populate via the data collection workflow.
