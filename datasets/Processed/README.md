# Processed Data

Signals after DC removal, filtering, and normalization (see `tinyml/Feature_Extraction/preprocess.py`), but before feature extraction.

**Status:** Empty — generated automatically when running the preprocessing pipeline against `Raw/`.
