# MQTT (Reference Module)

Documents the communication layer's MQTT responsibilities: WiFi management, MQTT publishing, packet creation, reconnection logic.

Implementation: `firmware/ESP32/communication/mqtt_manager.cpp`, `wifi_manager.cpp`, `packet_builder.cpp`.

Topic hierarchy and payload schema: see `docs/Communication_Architecture.md` and `communication/MQTT/README.md`.
